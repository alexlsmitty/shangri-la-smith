save-exact=true
legacy-peer-deps=true
prefer-offline=true
# Make sure dev dependencies are installed during build
production=false
# Ensure Vercel has access to all dependencies
registry=https://registry.npmjs.org/
